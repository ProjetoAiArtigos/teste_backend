{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "NODE_ENV=production npm run migrate && nodemon src/server.js",
    "dev": "NODE_ENV=development nodemon src/server.js",
    "migrate": "npx sequelize-cli db:migrate --config src/config/config.js --migrations-path src/migrations",
    "test": "NODE_ENV=test JWT_SECRET=test_secret npm run migrate:test && npx jest",
    "migrate:test": "NODE_ENV=test npx sequelize-cli db:migrate --env test --config src/config/config.js --migrations-path src/migrations"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-validator": "^7.2.0",
    "jsonwebtoken": "^9.0.2",
    "mysql2": "^3.11.5",
    "nodemon": "^3.1.7",
    "sequelize": "^6.37.5",
    "sequelize-cli": "^6.6.2"
  },
  "devDependencies": {
    "@babel/core": "^7.26.0",
    "@babel/preset-env": "^7.26.0",
    "@jest/globals": "^29.7.0",
    "babel-jest": "^29.7.0",
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  }
}
